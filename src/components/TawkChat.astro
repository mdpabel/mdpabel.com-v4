
<script>
  // This function ensures the script only runs once per page load
  // or after a view transition swap.
  function loadTawk() {
    var Tawk_API = (window as any).Tawk_API || {}, 
        Tawk_LoadStart = new Date();
    
    (function() {
      var s1 = document.createElement("script"),
          s0 = document.getElementsByTagName("script")[0];
      s1.async = true;
      s1.src = 'https://embed.tawk.to/68b076a8beccc01925d92112/1j3okv147';
      s1.charset = 'UTF-8';
      s1.setAttribute('crossorigin', '*');
      if (s0 && s0.parentNode) {
        s0.parentNode.insertBefore(s1, s0);
      }
    })();
  }

  // Load immediately on page load
  loadTawk();

  // If you use Astro View Transitions, re-initialize after navigation
  document.addEventListener('astro:after-swap', loadTawk);
</script>